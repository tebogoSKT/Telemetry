import { Router, CanActivate, ActivatedRouteSnapshot, RouterStateSnapshot } from '@angular/router';
import { GlobalVarsService } from '../../app/services/globalvars.service';
import { LocalStorageService } from 'angular-2-local-storage';
export declare class AuthIntercept implements CanActivate {
    private router;
    private globalservice;
    private localStorage;
    constructor(router: Router, globalservice: GlobalVarsService, localStorage: LocalStorageService);
    canActivate(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): boolean;
}
