import { Observable } from 'rxjs/Observable';
import 'rxjs/add/observable/throw';
import 'rxjs/add/operator/catch';
import 'rxjs/add/operator/map';
import { LogIn } from '../classes/login';
import { User } from '../classes/user';
import { GlobalVarsService } from '../../app/services/globalvars.service';
import { LocalStorageService } from 'angular-2-local-storage';
import { HttpClient } from '@angular/common/http';
export declare class AuthenticationService {
    private http;
    private globalservice;
    private localstorage;
    private logindata;
    private localStorage;
    private user;
    AuthURI: string;
    constructor(http: HttpClient, globalservice: GlobalVarsService, localstorage: LocalStorageService);
    login(logindata: LogIn): Observable<User>;
    logout(): void;
    private authData(res);
    private handleError(error);
    isAuthenticated(): boolean;
}
